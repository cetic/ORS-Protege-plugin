
    /* ***************************************************
     * Data Property ${propertyIri}
     * Range: ${propertyRange}
     * Range for class: ${propertyRangeForClass}
     * Range for impl: ${propertyRangeImplementation}
     */
     
    ${propertyRange} ${owlProperty};

    public ${propertyRange} get${OwlProperty}() {
		return this.${owlProperty};
    }

    public void  set${OwlProperty}(${propertyRange} value) {
		this.${owlProperty}=value;
    }


