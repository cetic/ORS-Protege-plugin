
    /* ***************************************************
     * Object Property ${propertyIri}
     */

    Collection<${propertyRange}> ${owlProperty}; 
    public Collection<${propertyRange}> get${OwlProperty}() {
        if (this.${owlProperty}==null)
            this.${owlProperty} =new ArrayList<>();
        return this.${owlProperty};
    }

    public void set${OwlProperty}(Collection<${propertyRange}> value) {
         this.${owlProperty}=value;
    }

